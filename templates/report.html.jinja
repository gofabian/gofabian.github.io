<!DOCTYPE html>
<html>
<head>
    <title>Stock Screener - {{ title }}</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">

    <style>
        h1 a {
            text-decoration: none;
        }

        table {
            margin-top: 30px;
            border-spacing: 0;
        }

        td, th {
            padding: 10px;
            text-align: left;
        }

        td a:focus {
            color: red;
        }
    </style>
</head>
<body>

<div class="w3-container w3-green">
    <h1><a href="#" onclick="window.location.reload();">{{ title }}</a></h1>
</div>

<div class="w3-row-padding">
    <div class="w3-half">
        <h2>Long-Signale ⇗ {{ symbols_long | length }}</h2>
        <p>{{ ', '.join(symbols_long) }}</p>

        <table>
            {% for symbol in symbols_long %}
                <tr>
                    <th>{{ symbol }}</th>
                    <td><a href="{{ symbol }}/chart.html">Chart</a></td>
                    <td><a href="https://de.tradingview.com/chart/?symbol={{ symbol }}">TradingView</a></td>
                </tr>
            {% endfor %}
        </table>

    </div>

    <div class="w3-half">
        <h2>Short-Signale ⇘ {{ symbols_short | length }}</h2>
        <p>{{ ', '.join(symbols_short) }}</p>

        <table>
            {% for symbol in symbols_short %}
                <tr>
                    <th>{{ symbol }}</th>
                    <td><a href="{{ symbol }}/chart.html">Chart</a></td>
                    <td><a href="https://de.tradingview.com/chart/?symbol={{ symbol }}">TradingView</a></td>
                </tr>
            {% endfor %}
        </table>
    </div>

</div>

</body>
</html>